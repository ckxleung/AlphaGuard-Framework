# Generated Outputs

Runtime reports are generated by CI or local execution and are not evidence of
live monitoring unless their source references and publication sidecar pass the
repository contracts.

The scheduled workflow uploads its portfolio routing plan as a GitHub Actions
artifact. Planning-only and synthetic runs are always marked
`publication_eligible: false`.

Handwritten reports must not be presented as automated production output.
